parameters:
    git_dir: .
    bin_dir: vendor/bin
    ascii:
        failed: ~
        succeeded: ~
    tasks:
        phplint:
            triggered_by: ['php', 'inc', 'phtml', 'html']
        phpcs:
            encoding: UTF-8
            standard: phpcs.xml